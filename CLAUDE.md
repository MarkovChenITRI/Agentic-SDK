## Agent skills

### Issue tracker

Local markdown under `.scratch/` (this repo tracks its own issues, separate from the parent folder). See `docs/agents/issue-tracker.md`.

### Triage labels

Default five canonical roles kept as-is. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context layout (`CONTEXT.md` + `docs/adr/` at repo root). See `docs/agents/domain.md`.
