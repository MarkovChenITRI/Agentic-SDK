# 貢獻指南

本專案接受工業技術研究院各單位與外部貢獻者的修改。提交前請先讀完本頁，
其中授權一節涉及技術移轉，與程式碼品質無關但同樣必要。

## 貢獻的授權

送出 Pull Request 即表示同意下列兩項：

1. 貢獻者擁有該修改的著作權，或已取得權利人授權提交。
2. 貢獻者授予工業技術研究院不可撤回的權利，得以任何條款再授權該修改，
   包含技術移轉的商業授權在內。

第二項是必要條件。本專案以 PolyForm Noncommercial 釋出，商業使用另循工研院技轉，
技轉標的必須涵蓋全部原始碼，其中任何一段不可再授權都會使整包無法移轉。

## 標示貢獻單位

貢獻紀錄分散在四份檔案，各自的粒度與更新時機不同。四份都是人寫的，
因此更新動作屬於帶來該次改動的那個 Pull Request，不另設流程。

| 檔案 | 粒度 | 什麼時候更新 | 誰更新 |
| --- | --- | --- | --- |
| 提交訊息的 `Co-authored-by:` 尾標 | 個人、逐次提交 | 每一次提交 | 提交者 |
| `CONTRIBUTORS.md` 貢獻人表 | 人 × 單位 × 期間 | 新貢獻人首次貢獻，或既有貢獻人換單位 | 送出該 PR 的人 |
| `CONTRIBUTORS.md` 單位表 | 單位、貢獻範疇 | 新單位首次貢獻，或既有單位的範疇擴大 | 送出該 PR 的人 |
| `CITATION.cff` | 單位、作者身分 | 新單位首次貢獻 | 送出該 PR 的人 |
| `.github/CODEOWNERS` | 路徑、審查責任 | 某單位開始負責一段路徑的審查 | 維護者 |
| `THIRD-PARTY-NOTICES.md` | 依賴 | 依賴有增減 | 送出該 PR 的人 |
| `REUSE.toml` | 功能、路徑 | 某單位的功能新增或搬移路徑 | 送出該 PR 的人 |

前四項靠 Pull Request 模板的檢查清單提醒。後兩項由 CI 檢查：改動依賴卻沒有
重新產生清單時建置失敗，`REUSE.toml` 宣告的路徑找不到檔案時同樣失敗。

### 功能歸屬怎麼宣告

`REUSE.toml` 用 SPDX 的著作權欄位記錄哪一段路徑屬於哪一個單位，機器可讀，
技轉作業據此界定標的。預設全部歸電光所，語音相關路徑另一個區塊覆寫為機械所。

新單位貢獻一整塊功能時，新增一個 `[[annotations]]` 區塊，`precedence` 填
`override`，`path` 列出該功能的檔案，`SPDX-FileCopyrightText` 填單位全名。
零星修改既有檔案不需要新增區塊，該檔案的歸屬不因此改變。

### 提交尾標的寫法

一次提交有多位作者時，尾標一人一行，放在提交訊息最末，前面空一行。

```
Co-authored-by: 姓名 <信箱>
```

信箱要與該人 GitHub 帳號上的信箱一致，圖表才算得到他。單位配發的信箱優於
GitHub 的 noreply 位址：該次提交的隸屬單位因而留在歷史裡，日後人事異動也不影響。

### 換單位怎麼記

同一個帳號換部門時，在 `CONTRIBUTORS.md` 的貢獻人表新增一列，填上新單位與新的
起始月份，並在舊列補上結束月份。舊列不改寫——那段期間的貢獻歸屬於當時的單位，
技轉標的的界定依此判讀。

### 依賴清單怎麼重新產生

```bash
python scripts/generate_third_party_notices.py
```

## 送出之前

| 項目 | 指令 |
| --- | --- |
| 全套測試 | `python -m pytest` |
| 文件站建置 | `mkdocs build --strict` |

新增依賴時一併更新 `THIRD-PARTY-NOTICES.md`。帶 copyleft 的授權需先討論，
本專案目前的依賴全部是寬鬆授權。
